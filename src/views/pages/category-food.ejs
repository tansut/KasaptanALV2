<!DOCTYPE html>
<html lang="en">

<head>
  <% include ./_partial/head %>
</head>

<body>
  <% include ./_partial/header %>



  <div class="page-title-overlap bg-primary pt-4">
    <div class="container d-lg-flex justify-content-between py-2 py-lg-3">
      <div class="order-lg-2 mb-3 mb-lg-0 pt-lg-2">
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb breadcrumb-light flex-lg-nowrap justify-content-center justify-content-lg-star">
            <li class="breadcrumb-item"><a class="text-nowrap" href="/"><i class="czi-home"></i></a></li>
            <li class="breadcrumb-item text-nowrap"><a href="/et-yemekleri">Siz Yemeği Seçin</a>
            </li>
            <li class="breadcrumb-item text-nowrap active" aria-current="page"><%= controller.category.name %></li>
          </ol>
        </nav>
      </div>
      <div class="media media-ie-fix d-block d-sm-flex align-items-center text-center text-sm-left">
        <a class="d-inline-block mx-auto mr-sm-4" href="/<%= controller.category.slug%>" style="width: 10rem;">
          
          <img class="rounded-circle" src="/<%= controller.category.slug%>/resimler/thumbnail" alt="<%= controller.category.name%>"></a>
        <div class="media-body pt-2">
 
 
          <h1 class="h3 text-light mb-0"><%= controller.category.name %></h1>
          <p class="lead text-light"><%= controller.category.shortdesc %></p>

        </div>
      </div>
    </div>
  </div>

  <% var reyonlar = __categories.filter(p=>p.type == 'reyon').slice(0,2) %>
  <div class="container px-0">
    <div class="bg-light box-shadow-lg  px-4 py-4 mb-3 __xxx__">
      <!-- <ul class="nav nav-tabs  nav-justified mb-3 __xxx__" id="pills-tab" role="tablist">
        <li class="nav-item">
          <a class="nav-link nav-link-shrink <%= __req.params.subcategory ? '': 'active' %>" id=all-tab" 
            href="/<%=  controller.category.slug  %>" role="tab" aria-controls="Tümü"
            >Tümü</a>
        </li>

        <%reyonlar.forEach((category,i) => { %>
        <li class="nav-item">
          <a class="nav-link nav-link-shrink <%= controller.category ? (__req.params.subcategory == category.slug ? 'active':''): '' %>" id="<%= category.slug %>-tab" 
            href="<%=`/${controller.category.slug}/alt/${category.slug}`%>" role="tab" aria-controls="<%= category.name %>"
            ><%= category.name %></a>
        </li>
        <% }) %>
      </ul> -->

      <div class="row cz-gallery">
        <% controller.foods.forEach((resource,i) => {%>
          <div class="col-md-2 col-6 mb-4">
          <%- include(`./_partial/resource-item.ejs`, {resource: resource, subUrl: 'ile-yapin'  }) %>

        </div>
        <% }) %>
      </div>




      <%- include('./_partial/category-slider', { viewCats: ['resource'], title: 'Yemek Kategorileri', allUrl: '/et-yemekleri' }); %>



    </div>
  </div>

  <% include ./_partial/footer %>
  <% include ./_partial/scripts %>

</body>

</html>