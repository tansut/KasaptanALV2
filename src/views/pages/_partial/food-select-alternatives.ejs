<div class="modal fade" style="z-index: 10555;" id="food-alternative-meats">
  <div class="modal-dialog modal-dialog-scrollable">
    <div class="modal-content">
      <div class="modal-header">
        <h6 class="modal-title">Hepsiyle güzel</h6>
        <button class="close" type="button" data-dismiss="modal" aria-label="Close"><span
            aria-hidden="true">&times;</span></button>
      </div>
      <div class="modal-body p-0">
        <div class="pt-2">
          <p class="p-0 text-center">
            <span class="font-weight-medium">{{title}}</span> için kasaplarımız aşağıdaki lezzetlerimizi önermekteler.
          </p>
          <div class="row no-gutters">
            <div class="col-12 mb-3" v-for="item in list">
              <div class="card border-bottom rounded-0 product-card card-static h-100 item-card">
                <a class=" d-flex overflow-hidden justify-content-center" :href="item.link"><img class="rounded-0" :src="item.src"
                    :alt="item.name"></a>
                <div class="card-body pl-0 text-center">
                  <h3 v-if="item && item.name" class="product-title font-size-md mb-1"><a :href="item.link">{{item.name}}</a>
                  </h3>
                  <p v-if="item && item.desc" class="card-text mb-0 text-muted font-size-sm">
                    {{item.desc}}
                  </p>

                </div>
      
              </div>
            </div>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>